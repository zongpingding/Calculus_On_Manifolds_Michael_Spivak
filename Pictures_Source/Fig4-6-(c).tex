\documentclass[10pt,varwidth]{standalone}
\PassOptionsToPackage{quiet}{fontspec}
\usepackage[a4paper, left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm]{geometry}
\usepackage{xifthen}
\usepackage{xfp}
\usepackage{xcolor}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\pgfplotsset{compat=1.16}
% 2.引用的tikz库
\usetikzlibrary {matrix, chains, trees, decorations}
\usetikzlibrary {arrows.meta, automata,positioning}
\usetikzlibrary {decorations.pathmorphing, calc}
\usetikzlibrary {calligraphy}
\usetikzlibrary {backgrounds, mindmap,shadows}
\usetikzlibrary {patterns, quotes, 3d, shadows}
\usetikzlibrary {graphs, fadings, scopes}
\usetikzlibrary {arrows, shapes.geometric}
\usepgflibrary {shadings}

\tikzset{
    >={Latex}
}



\begin{document}
\begin{tikzpicture}[font=\Large]
    % axis
    \draw[->] (-5, 0) -- (6, 0)    node [below] {$x$};
    \draw[->] (0, 0)  -- (0, 5)    node [left]  {$z$};
    \draw[->] (-135:4) -- (0, 0) -- (45:7.06) node [above] {$y$};
    % ellipse 1
    \draw plot[samples=100,domain=0:55,smooth,variable=\t] ({2*cos(\t)}, {sin(\t)});
    \draw plot[samples=100,domain=59:85,smooth,variable=\t] ({2*cos(\t)}, {sin(\t)});
    \draw plot[samples=100,domain=88:360,smooth,variable=\t] ({2*cos(\t)}, {sin(\t)});
    % ellipse 2
    \draw plot[samples=100,domain=-153:-15,smooth,variable=\t] ({cos(\t)+2}, {2*sin(\t)});
    \draw plot[samples=100,domain=-7:203,smooth,variable=\t] ({cos(\t)+2}, {2*sin(\t)});
    % ellipse 3
    \draw plot[samples=100,domain=-159:-8,smooth,variable=\t] ({2*cos(\t)+2}, {3*sin(\t)});
    \draw plot[samples=100,domain=-5:198,smooth,variable=\t] ({2*cos(\t)+2}, {3*sin(\t)});
    % connection part
    \draw (3.975, -0.46) to[out=80, in=60] (3.1, -0.3);
    \draw (2.992, -0.23)  to[out=-90, in=-120] (3.9, -0.42);
    % fig num
    \node at (0, -2.5) [below] {(c)};
\end{tikzpicture}
\end{document}